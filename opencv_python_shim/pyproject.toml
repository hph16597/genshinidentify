[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "opencv-python"
version = "4.13.0.92"
description = "Compatibility shim that uses opencv-python-headless on servers"
requires-python = ">=3.9"
dependencies = ["opencv-python-headless==4.13.0.92"]
